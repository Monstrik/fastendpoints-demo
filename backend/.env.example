# Copy this file to .env for local development.
# Do not commit real secrets.

# Required by backend JWT configuration (must be at least 32 characters for HS256)
Jwt__SigningKey=replace-with-a-long-random-secret-here!!
# Optional: enable Swagger/OpenAPI UI in local/dev environments
# ENABLE_SWAGGER=true
